# Tkinter-password-manager
Password Manager is a Python-based GUI application that enables users to generate random passwords, save website login details, and retrieve stored credentials through an easy-to-use interface.
